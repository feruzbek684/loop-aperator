#include <stdio.h>

int main (){
	
		int son, q = 0, w = 5, e = 0;
		do{
			scanf("%d",&son);
			if ( son > e){
				e = son ;
			}
		q ++;

		}while (q < w);
		
	printf("Katta son %d", e);
	return 0;	
	
}